<Window x:Class="Simple_Customized_Crosshair_SCC.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Simple_Customized_Crosshair_SCC.Converters"
        xmlns:models="clr-namespace:Simple_Customized_Crosshair_SCC.Models"
        Title="Crosshair Studio" 
        Height="800" 
        Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#0F0F0F"
        BorderBrush="#2A2A2A"
        BorderThickness="1"
        WindowStyle="None"
        AllowsTransparency="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="35"
                      ResizeBorderThickness="5"
                      GlassFrameThickness="0"
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        <converters:EnumToCollectionConverter x:Key="EnumToCollectionConverter"/>
        <converters:CrosshairStatusConverter x:Key="CrosshairStatusConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Modern Color Palette -->
        <Color x:Key="PrimaryColor">#6366F1</Color>
        <Color x:Key="PrimaryDarkColor">#4F46E5</Color>
        <Color x:Key="SuccessColor">#10B981</Color>
        <Color x:Key="DangerColor">#EF4444</Color>
        <Color x:Key="BackgroundColor">#0F0F0F</Color>
        <Color x:Key="CardColor">#1A1A1A</Color>
        <Color x:Key="BorderColor">#2A2A2A</Color>
        <Color x:Key="TextPrimary">#E5E5E5</Color>
        <Color x:Key="TextSecondary">#A3A3A3</Color>
        <Color x:Key="HoverColor">#2A2A2A</Color>
        <Color x:Key="ScrollBarColor">#404040</Color>
        <Color x:Key="ScrollBarThumbColor">#636363</Color>

        <!-- Modern Brushes -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
        <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}"/>
        <SolidColorBrush x:Key="ScrollBarBrush" Color="{StaticResource ScrollBarColor}"/>
        <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="{StaticResource ScrollBarThumbColor}"/>

        <!-- Window Control Button Styles -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E5E5E5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#404040"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowCloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Thumb Style for ScrollBar -->
        <Style x:Key="ModernThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{StaticResource ScrollBarThumbBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Vertical ScrollBar Style -->
        <Style x:Key="ModernVerticalScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Bg">
                            <Track x:Name="PART_Track" 
                                   IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb"
                                           Background="{StaticResource ScrollBarThumbBrush}"
                                           BorderBrush="{StaticResource BorderBrush}"
                                           BorderThickness="1"
                                           Style="{StaticResource ModernThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Thumb" Property="Background" Value="#7A7A7A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Horizontal ScrollBar Style -->
        <Style x:Key="ModernHorizontalScrollBar" TargetType="ScrollBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Bg">
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb"
                                           Background="{StaticResource ScrollBarThumbBrush}"
                                           BorderBrush="{StaticResource BorderBrush}"
                                           BorderThickness="1"
                                           Style="{StaticResource ModernThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Thumb" Property="Background" Value="#7A7A7A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollViewer Style -->
        <Style x:Key="ModernScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                  Grid.Row="0"
                                                  Grid.Column="0"
                                                  Margin="{TemplateBinding Padding}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  CanContentScroll="{TemplateBinding CanContentScroll}"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                     Grid.Row="0"
                                     Grid.Column="1"
                                     Orientation="Vertical"
                                     Value="{TemplateBinding VerticalOffset}"
                                     Maximum="{TemplateBinding ScrollableHeight}"
                                     ViewportSize="{TemplateBinding ViewportHeight}"
                                     Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                     Style="{StaticResource ModernVerticalScrollBar}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                     Grid.Row="1"
                                     Grid.Column="0"
                                     Orientation="Horizontal"
                                     Value="{TemplateBinding HorizontalOffset}"
                                     Maximum="{TemplateBinding ScrollableWidth}"
                                     ViewportSize="{TemplateBinding ViewportWidth}"
                                     Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                     Style="{StaticResource ModernHorizontalScrollBar}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="32"/>
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter x:Name="ContentSite"
                                                    Grid.Column="0"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Center"
                                                    Margin="{TemplateBinding Padding}"
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                    IsHitTestVisible="False"/>

                                    <ToggleButton x:Name="ToggleButton"
                                                Grid.Column="1"
                                                Focusable="False"
                                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                ClickMode="Press"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                BorderThickness="0">
                                        <ToggleButton.Template>
                                            <ControlTemplate>
                                                <Border Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        BorderThickness="0"
                                                        Width="32"
                                                        Height="36">
                                                    <Path x:Name="Arrow"
                                                          Data="M0,0 L4,4 8,0 Z"
                                                          Stretch="Uniform"
                                                          Fill="{StaticResource TextSecondaryBrush}"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="0,0,12,0"/>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup"
                                  Placement="Bottom"
                                  IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                  AllowsTransparency="True"
                                  PopupAnimation="Slide"
                                  Focusable="False">
                                <Grid x:Name="DropDown"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      SnapsToDevicePixels="True">
                                    <Border x:Name="DropDownBorder"
                                            Background="{StaticResource CardBrush}"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="6">
                                        <ScrollViewer Style="{StaticResource ModernScrollViewer}">
                                            <ItemsPresenter x:Name="ItemsPresenter"
                                                          KeyboardNavigation.DirectionalNavigation="Contained"
                                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="Height" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="True">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            SnapsToDevicePixels="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Styles -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0F9D6E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern GroupBox Style -->
        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0" 
                                            Background="{StaticResource BackgroundBrush}"
                                            Margin="12,-10,12,0"
                                            Padding="8,0">
                                        <TextBlock Text="{TemplateBinding Header}"
                                                   Foreground="{TemplateBinding Foreground}"
                                                   FontWeight="Bold"
                                                   FontSize="13"
                                                   Margin="4,2"
                                                   HorizontalAlignment="Left"/>
                                    </Border>

                                    <ContentPresenter Grid.Row="1" 
                                                    Margin="{TemplateBinding Padding}"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Advanced Settings GroupBox Style with Conditional Visibility -->
        <Style x:Key="AdvancedSettingsGroupBox" TargetType="GroupBox" BasedOn="{StaticResource ModernGroupBox}">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentCrosshair.ShowCenterDot}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentCrosshair.ShowCrosshairLines}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentCrosshair.ShowOutline}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Modern CheckBox Style -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="checkBoxBorder"
                                    Grid.Column="0"
                                    Width="16"
                                    Height="16"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    VerticalAlignment="Center">
                                <Path x:Name="checkMark"
                                      Data="M0,5 L3,8 8,0"
                                      Stretch="Uniform"
                                      Fill="{StaticResource PrimaryBrush}"
                                      StrokeThickness="2"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Opacity="0"/>
                            </Border>

                            <TextBlock Grid.Column="1"
                                      Text="{TemplateBinding Content}"
                                      Foreground="{TemplateBinding Foreground}"
                                      Margin="8,0,0,0"
                                      VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Opacity" Value="1"/>
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Slider Style -->
        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Height" Value="22"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Slider">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto" MinHeight="4"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Track Grid.Row="1" Name="PART_Track">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="Slider.DecreaseLarge" 
                                                         Background="{StaticResource BorderBrush}"
                                                         BorderThickness="0"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Background="{StaticResource PrimaryBrush}"
                                                   BorderBrush="{StaticResource PrimaryDarkBrush}"
                                                   BorderThickness="1"
                                                   Width="12"
                                                   Height="12">
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Ellipse Fill="{TemplateBinding Background}"
                                                                 Stroke="{TemplateBinding BorderBrush}"
                                                                 StrokeThickness="{TemplateBinding BorderThickness}"/>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="Slider.IncreaseLarge" 
                                                         Background="{StaticResource BorderBrush}"
                                                         BorderThickness="0"/>
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBlock Styles -->
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SectionHeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="BodyText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Status Indicator Style -->
        <Style x:Key="StatusIndicator" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="16,12"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" 
                Background="#1A1A1A" 
                Height="35"
                BorderBrush="#2A2A2A"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title Text -->
                <TextBlock Grid.Column="0" 
                          Text="Crosshair Studio" 
                          Foreground="#E5E5E5" 
                          FontSize="12"
                          VerticalAlignment="Center"
                          Margin="15,0,0,0"/>

                <!-- Window Controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Background="Transparent">
                    <Button x:Name="MinimizeButton"
                            Style="{StaticResource WindowControlButton}"
                            Content="─"
                            ToolTip="Minimize"
                            Click="MinimizeButton_Click"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>

                    <Button x:Name="MaximizeButton"
                            Style="{StaticResource WindowControlButton}"
                            Content="□"
                            ToolTip="Maximize"
                            Click="MaximizeButton_Click"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>

                    <Button x:Name="CloseButton"
                            Style="{StaticResource WindowCloseButton}"
                            Content="×"
                            ToolTip="Close"
                            Click="CloseButton_Click"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Border Grid.Row="1" 
                Background="{StaticResource BackgroundBrush}"
                Margin="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" 
                        Background="Transparent" 
                        Margin="0,0,0,20">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <TextBlock Text="CROSSHAIR STUDIO" 
                                  Style="{StaticResource HeaderText}"
                                  Foreground="{StaticResource PrimaryBrush}"
                                  Margin="0,0,0,4"/>
                        <TextBlock Text="Precision targeting customization"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  FontSize="14"
                                  TextAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Content Area -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="420"/>
                    </Grid.ColumnDefinitions>

                    <!-- Preview Area -->
                    <Border Grid.Column="0" 
                            Background="{StaticResource CardBrush}" 
                            CornerRadius="8" 
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1">
                        <Grid>
                            <Border Background="{StaticResource BackgroundBrush}" 
                                    Margin="24"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1">
                                <Canvas x:Name="MainPreviewCanvas" Background="Transparent"/>
                            </Border>
                            <TextBlock Text="LIVE PREVIEW" 
                                      Style="{StaticResource SectionHeaderText}"
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Top" 
                                      Margin="16,12"
                                      Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Grid>
                    </Border>

                    <!-- Controls Panel -->
                    <ScrollViewer Grid.Column="2" 
                                 Style="{StaticResource ModernScrollViewer}"
                                 Padding="0,0,2,0">
                        <StackPanel>

                            <!-- Crosshair Selection -->
                            <GroupBox Header="CROSSHAIR SELECTION" 
                                     Style="{StaticResource ModernGroupBox}">
                                <StackPanel>
                                    <ComboBox ItemsSource="{Binding Crosshairs}"
                                             DisplayMemberPath="Name"
                                             SelectedItem="{Binding CurrentCrosshair}"
                                             Style="{StaticResource ModernComboBox}">
                                        <ComboBox.ItemContainerStyle>
                                            <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ModernComboBoxItem}"/>
                                        </ComboBox.ItemContainerStyle>
                                    </ComboBox>

                                    <StackPanel Orientation="Horizontal" 
                                               Margin="0,12,0,0" 
                                               HorizontalAlignment="Center">
                                        <Button Content="New" 
                                               Command="{Binding NewCrosshairCommand}" 
                                               Style="{StaticResource PrimaryButton}" 
                                               Width="70" Margin="4"/>
                                        <Button Content="Rename" 
                                               Command="{Binding RenameCrosshairCommand}" 
                                               Style="{StaticResource ModernButton}" 
                                               Width="75" Margin="4"/>
                                        <Button Content="Duplicate" 
                                               Command="{Binding DuplicateCrosshairCommand}" 
                                               Style="{StaticResource ModernButton}" 
                                               Width="85" Margin="4"/>
                                        <Button Content="Delete" 
                                               Command="{Binding DeleteCrosshairCommand}" 
                                               Style="{StaticResource DangerButton}" 
                                               Width="70" Margin="4"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>

                            <!-- Crosshair Type -->
                            <GroupBox Header="CROSSHAIR TYPE" 
                                     Style="{StaticResource ModernGroupBox}">
                                <ComboBox ItemsSource="{Binding Source={x:Type models:CrosshairType}, Converter={StaticResource EnumToCollectionConverter}}"
                                         SelectedValue="{Binding CurrentCrosshair.Type}"
                                         Style="{StaticResource ModernComboBox}">
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ModernComboBoxItem}"/>
                                    </ComboBox.ItemContainerStyle>
                                </ComboBox>
                            </GroupBox>

                            <!-- Basic Appearance -->
                            <GroupBox Header="BASIC APPEARANCE" 
                                     Style="{StaticResource ModernGroupBox}">
                                <StackPanel>
                                    <!-- Color Picker -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Color" 
                                                  Style="{StaticResource LabelText}"
                                                  Grid.Column="0" 
                                                  Margin="0,0,12,0"/>
                                        <Button Grid.Column="2" 
                                                Content="Pick"
                                                Command="{Binding ChangeColorCommand}"
                                                Style="{StaticResource ModernButton}" 
                                                Height="28" 
                                                Padding="8,4"/>
                                        <Border Grid.Column="2" 
                                                Background="{Binding CurrentCrosshair.Color, Converter={StaticResource ColorToBrushConverter}}"
                                                BorderBrush="{StaticResource BorderBrush}" 
                                                BorderThickness="1"
                                                Width="20" 
                                                Height="20"
                                                CornerRadius="4"
                                                Margin="32,0,0,0"/>
                                    </Grid>

                                    <!-- Size -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Size" 
                                                  Style="{StaticResource LabelText}"
                                                  Grid.Column="0" 
                                                  Margin="0,0,12,0"/>
                                        <Slider Grid.Column="1" 
                                               Minimum="5" 
                                               Maximum="100" 
                                               Value="{Binding CurrentCrosshair.Size}"
                                               Style="{StaticResource ModernSlider}"
                                               Margin="4"/>
                                        <TextBlock Text="{Binding CurrentCrosshair.Size, StringFormat={}{0:F0}}" 
                                                  Style="{StaticResource LabelText}"
                                                  Grid.Column="2" 
                                                  HorizontalAlignment="Right"/>
                                    </Grid>

                                    <!-- Thickness -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Thickness" 
                                                  Style="{StaticResource LabelText}"
                                                  Grid.Column="0" 
                                                  Margin="0,0,12,0"/>
                                        <Slider Grid.Column="1" 
                                               Minimum="1" 
                                               Maximum="10" 
                                               Value="{Binding CurrentCrosshair.Thickness}"
                                               Style="{StaticResource ModernSlider}"
                                               Margin="4"/>
                                        <TextBlock Text="{Binding CurrentCrosshair.Thickness, StringFormat={}{0:F0}}" 
                                                  Style="{StaticResource LabelText}"
                                                  Grid.Column="2" 
                                                  HorizontalAlignment="Right"/>
                                    </Grid>

                                    <!-- Opacity -->
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Opacity" 
                                                  Style="{StaticResource LabelText}"
                                                  Grid.Column="0" 
                                                  Margin="0,0,12,0"/>
                                        <Slider Grid.Column="1" 
                                               Minimum="0.1" 
                                               Maximum="1" 
                                               Value="{Binding CurrentCrosshair.Opacity}"
                                               Style="{StaticResource ModernSlider}"
                                               Margin="4"/>
                                        <TextBlock Text="{Binding CurrentCrosshair.Opacity, StringFormat={}{0:P0}}" 
                                                  Style="{StaticResource LabelText}"
                                                  Grid.Column="2" 
                                                  HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>
                            </GroupBox>

                            <!-- Advanced Custom Crosshair Settings -->
                            <GroupBox Header="ADVANCED SETTINGS" 
                                     Style="{StaticResource AdvancedSettingsGroupBox}">
                                <StackPanel>

                                    <!-- Center Dot Settings -->
                                    <Border Background="{StaticResource CardBrush}" 
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            Margin="0,0,0,8"
                                            Padding="12"
                                            CornerRadius="6">
                                        <StackPanel>
                                            <TextBlock Text="Center Dot" 
                                                      Style="{StaticResource SectionHeaderText}"
                                                      Margin="0,0,0,8"/>
                                            <CheckBox Content="Show Center Dot" 
                                                     IsChecked="{Binding CurrentCrosshair.ShowCenterDot}"
                                                     Style="{StaticResource ModernCheckBox}" 
                                                     Margin="0,0,0,8"/>

                                            <Grid Margin="0,4,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowCenterDot}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="60"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Dot Color" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="0" 
                                                          Margin="0,0,12,0"/>
                                                <Button Grid.Column="2" 
                                                        Content="Pick"
                                                        Command="{Binding ChangeDotColorCommand}"
                                                        Style="{StaticResource ModernButton}" 
                                                        Height="24" 
                                                        Padding="6,2"/>
                                                <Border Grid.Column="2" 
                                                        Background="{Binding CurrentCrosshair.DotColor, Converter={StaticResource ColorToBrushConverter}}"
                                                        BorderBrush="{StaticResource BorderBrush}" 
                                                        BorderThickness="1"
                                                        Width="16" 
                                                        Height="16"
                                                        CornerRadius="3"
                                                        Margin="32,0,0,0"/>
                                            </Grid>

                                            <!-- Dot Size -->
                                            <Grid Margin="0,8,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowCenterDot}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="40"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Dot Size" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="0" 
                                                          Margin="0,0,12,0"/>
                                                <Slider Grid.Column="1" 
                                                       Minimum="1" 
                                                       Maximum="20" 
                                                       Value="{Binding CurrentCrosshair.DotSize}"
                                                       Style="{StaticResource ModernSlider}"
                                                       Margin="4"/>
                                                <TextBlock Text="{Binding CurrentCrosshair.DotSize, StringFormat={}{0:F0}}" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="2" 
                                                          HorizontalAlignment="Right"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- Crosshair Lines Settings -->
                                    <Border Background="{StaticResource CardBrush}" 
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            Margin="0,0,0,8"
                                            Padding="12"
                                            CornerRadius="6">
                                        <StackPanel>
                                            <TextBlock Text="Crosshair Lines" 
                                                      Style="{StaticResource SectionHeaderText}"
                                                      Margin="0,0,0,8"/>
                                            <CheckBox Content="Show Crosshair Lines" 
                                                     IsChecked="{Binding CurrentCrosshair.ShowCrosshairLines}"
                                                     Style="{StaticResource ModernCheckBox}" 
                                                     Margin="0,0,0,8"/>

                                            <Grid Margin="0,8,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowCrosshairLines}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="60"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Line Color" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="0" 
                                                          Margin="0,0,12,0"/>
                                                <Button Grid.Column="2" 
                                                        Content="Pick"
                                                        Command="{Binding ChangeLineColorCommand}"
                                                        Style="{StaticResource ModernButton}" 
                                                        Height="24" 
                                                        Padding="6,2"/>
                                                <Border Grid.Column="2" 
                                                        Background="{Binding CurrentCrosshair.LineColor, Converter={StaticResource ColorToBrushConverter}}"
                                                        BorderBrush="{StaticResource BorderBrush}" 
                                                        BorderThickness="1"
                                                        Width="16" 
                                                        Height="16"
                                                        CornerRadius="3"
                                                        Margin="32,0,0,0"/>
                                            </Grid>

                                            <!-- Line Length -->
                                            <Grid Margin="0,8,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowCrosshairLines}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="40"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Line Length" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="0" 
                                                          Margin="0,0,12,0"/>
                                                <Slider Grid.Column="1" 
                                                       Minimum="2" 
                                                       Maximum="50" 
                                                       Value="{Binding CurrentCrosshair.LineLength}"
                                                       Style="{StaticResource ModernSlider}"
                                                       Margin="4"/>
                                                <TextBlock Text="{Binding CurrentCrosshair.LineLength, StringFormat={}{0:F0}}" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="2" 
                                                          HorizontalAlignment="Right"/>
                                            </Grid>

                                            <!-- Line Thickness -->
                                            <Grid Margin="0,8,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowCrosshairLines}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="40"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Line Thickness" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="0" 
                                                          Margin="0,0,12,0"/>
                                                <Slider Grid.Column="1" 
                                                       Minimum="1" 
                                                       Maximum="10" 
                                                       Value="{Binding CurrentCrosshair.LineThickness}"
                                                       Style="{StaticResource ModernSlider}"
                                                       Margin="4"/>
                                                <TextBlock Text="{Binding CurrentCrosshair.LineThickness, StringFormat={}{0:F0}}" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="2" 
                                                          HorizontalAlignment="Right"/>
                                            </Grid>

                                            <!-- Gap -->
                                            <Grid Margin="0,8,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowCrosshairLines}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="40"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Gap" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="0" 
                                                          Margin="0,0,12,0"/>
                                                <Slider Grid.Column="1" 
                                                       Minimum="0" 
                                                       Maximum="20" 
                                                       Value="{Binding CurrentCrosshair.Gap}"
                                                       Style="{StaticResource ModernSlider}"
                                                       Margin="4"/>
                                                <TextBlock Text="{Binding CurrentCrosshair.Gap, StringFormat={}{0:F0}}" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="2" 
                                                          HorizontalAlignment="Right"/>
                                            </Grid>

                                            <!-- Individual Line Visibility -->
                                            <StackPanel Orientation="Horizontal" 
                                                       Margin="0,12,0,0" 
                                                       HorizontalAlignment="Center">
                                                <StackPanel.Style>
                                                    <Style TargetType="StackPanel">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowCrosshairLines}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </StackPanel.Style>
                                                <StackPanel>
                                                    <CheckBox Content="Top" 
                                                             IsChecked="{Binding CurrentCrosshair.ShowTopLine}" 
                                                             Style="{StaticResource ModernCheckBox}" 
                                                             Margin="2"/>
                                                    <CheckBox Content="Bottom" 
                                                             IsChecked="{Binding CurrentCrosshair.ShowBottomLine}" 
                                                             Style="{StaticResource ModernCheckBox}" 
                                                             Margin="2"/>
                                                </StackPanel>
                                                <StackPanel Margin="12,0,0,0">
                                                    <CheckBox Content="Left" 
                                                             IsChecked="{Binding CurrentCrosshair.ShowLeftLine}" 
                                                             Style="{StaticResource ModernCheckBox}" 
                                                             Margin="2"/>
                                                    <CheckBox Content="Right" 
                                                             IsChecked="{Binding CurrentCrosshair.ShowRightLine}" 
                                                             Style="{StaticResource ModernCheckBox}" 
                                                             Margin="2"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Outline Settings -->
                                    <Border Background="{StaticResource CardBrush}" 
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            Padding="12"
                                            CornerRadius="6">
                                        <StackPanel>
                                            <TextBlock Text="Outline" 
                                                      Style="{StaticResource SectionHeaderText}"
                                                      Margin="0,0,0,8"/>
                                            <CheckBox Content="Show Outline" 
                                                     IsChecked="{Binding CurrentCrosshair.ShowOutline}"
                                                     Style="{StaticResource ModernCheckBox}" 
                                                     Margin="0,0,0,8"/>

                                            <Grid Margin="0,8,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowOutline}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="60"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Outline Color" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="0" 
                                                          Margin="0,0,12,0"/>
                                                <Button Grid.Column="2" 
                                                        Content="Pick"
                                                        Command="{Binding ChangeOutlineColorCommand}"
                                                        Style="{StaticResource ModernButton}" 
                                                        Height="24" 
                                                        Padding="6,2"/>
                                                <Border Grid.Column="2" 
                                                        Background="{Binding CurrentCrosshair.OutlineColor, Converter={StaticResource ColorToBrushConverter}}"
                                                        BorderBrush="{StaticResource BorderBrush}" 
                                                        BorderThickness="1"
                                                        Width="16" 
                                                        Height="16"
                                                        CornerRadius="3"
                                                        Margin="32,0,0,0"/>
                                            </Grid>

                                            <!-- Outline Thickness -->
                                            <Grid Margin="0,8,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentCrosshair.ShowOutline}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="40"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Outline Thickness" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="0" 
                                                          Margin="0,0,12,0"/>
                                                <Slider Grid.Column="1" 
                                                       Minimum="1" 
                                                       Maximum="5" 
                                                       Value="{Binding CurrentCrosshair.OutlineThickness}"
                                                       Style="{StaticResource ModernSlider}"
                                                       Margin="4"/>
                                                <TextBlock Text="{Binding CurrentCrosshair.OutlineThickness, StringFormat={}{0:F0}}" 
                                                          Style="{StaticResource LabelText}"
                                                          Grid.Column="2" 
                                                          HorizontalAlignment="Right"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </GroupBox>

                            <!-- Display Settings -->
                            <GroupBox Header="DISPLAY SETTINGS" 
                                     Style="{StaticResource ModernGroupBox}">
                                <StackPanel>
                                    <ComboBox ItemsSource="{Binding Screens}"
                                             DisplayMemberPath="Name"
                                             SelectedIndex="{Binding DisplaySettings.SelectedMonitorIndex}"
                                             Style="{StaticResource ModernComboBox}">
                                        <ComboBox.ItemContainerStyle>
                                            <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ModernComboBoxItem}"/>
                                        </ComboBox.ItemContainerStyle>
                                    </ComboBox>

                                    <CheckBox Content="Always on Top" 
                                             IsChecked="{Binding DisplaySettings.AlwaysOnTop}"
                                             Style="{StaticResource ModernCheckBox}" 
                                             Margin="0,12,0,0"/>

                                    <CheckBox Content="Click Through" 
                                             IsChecked="{Binding DisplaySettings.ClickThrough}"
                                             Style="{StaticResource ModernCheckBox}" 
                                             Margin="0,8,0,0"/>
                                </StackPanel>
                            </GroupBox>

                            <!-- Control Buttons -->
                            <StackPanel Orientation="Horizontal" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,20,0,0">
                                <Button Content="Show Crosshair" 
                                       Command="{Binding ShowCrosshairCommand}"
                                       Style="{StaticResource SuccessButton}" 
                                       Width="140" 
                                       Margin="4" 
                                       FontSize="13"/>
                                <Button Content="Hide Crosshair" 
                                       Command="{Binding HideCrosshairCommand}"
                                       Style="{StaticResource DangerButton}" 
                                       Width="140" 
                                       Margin="4"
                                       FontSize="13"/>
                            </StackPanel>

                            <!-- Save/Export Buttons -->
                            <StackPanel Orientation="Horizontal" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,16,0,0">
                                <Button Content="Save" 
                                       Command="{Binding SaveCrosshairCommand}"
                                       Style="{StaticResource PrimaryButton}" 
                                       Width="80" Margin="4"/>
                                <Button Content="Export" 
                                       Command="{Binding ExportCrosshairCommand}"
                                       Style="{StaticResource ModernButton}" 
                                       Width="80" Margin="4"/>
                                <Button Content="Import" 
                                       Command="{Binding ImportCrosshairCommand}"
                                       Style="{StaticResource ModernButton}" 
                                       Width="80" Margin="4"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <!-- Status Footer -->
                <Border Grid.Row="2" 
                        Style="{StaticResource StatusIndicator}"
                        Margin="0,16,0,0">
                    <TextBlock Text="{Binding IsCrosshairVisible, Converter={StaticResource CrosshairStatusConverter}}" 
                              Foreground="{StaticResource SuccessBrush}" 
                              HorizontalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>